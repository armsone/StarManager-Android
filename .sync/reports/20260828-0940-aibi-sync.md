# StarManager AIBI sync report — 2026-08-28 09:40 KST

## Scope

- Sync group: `starmanager` (`StarManager` iPhone reference → `StarManager-Android` target).
- Applied the portable **AIBI — AI Browser Interface** contract without changing product version/build.
- No commit, push, install, release, TestFlight, GitHub Release, or NasFinder.com work was performed.

## iPhone reference findings

- Active providers are Gemini, ChatGPT, and Claude; Grok remains excluded from the user roster.
- Browser visibility defaults off. Hidden execution uses an attached 375×667 surface at opacity 0.001 and displays progress below the provider buttons.
- Login and generation surfaces share the platform web-session store. Hidden fallback destroys the old browser before a fresh visible browser is created.
- Readiness is 35 s / 700 ms / 12 consecutive misses; submission is 15 s with click → pointer/mouse → form → Enter escalation and 700 ms verification; visible autofill is 45 s; completion is an initial non-empty inactive answer plus two further identical 700 ms readings.
- Provider/network errors sanitize and abort; they do not cause hidden-to-visible fallback.

## Android changes

- Preserved the existing Compose/WebView structure and upgraded its selector, readiness, injection, submit, verification, extraction, stability, error, cancellation, clipboard, and takeover behavior.
- Removed the arbitrary 15-character answer minimum and aligned three-reading stability.
- Added exact HTTPS host policy with separate script and authentication origins; scripts cannot run on authentication origins.
- Added density-aware attached hidden WebView behavior, persistent CookieManager/WebStorage reuse, live under-button progress, and fresh visible takeover.
- Kept StarManager Instagram/content validation in Composer/ViewModel host code. Media attachment remains visible user interaction only when browser file selection is needed.

## Intentional difference

- Android uses an explicit hardened script/auth origin allowlist. The older iPhone implementation historically permitted general navigation; this hardening is the portable AIBI default and is not treated as a product regression.

## Verification

- `./gradlew testDebugUnitTest --tests com.armsone.starmanager.ExternalAIParityTest --tests com.armsone.starmanager.ExternalAIDraftUtilsTest` — passed.
- `./gradlew assembleDebug` — passed.
- YAML and JSON syntax checks — passed.
- Matchup runtime/visual gate — **OPEN**. No current deterministic paired iPhone/Android captures were produced in this task. Live login persistence, provider DOM changes, CAPTCHA takeover, and end-to-end result import must be checked on devices before claiming full rendered parity.

## Login-state correction — 10:11–10:24 KST

- User evidence reopened the login-management contract: both platforms used the ambiguous static `로그인 열기`, and Android retained `로그인이 필요해요` over an already authenticated ChatGPT page without leaving the surface.
- iOS and Android now use the same concise states: `확인 중`, `로그인됨`, `로그인 필요`.
- `로그인됨` requires positive authenticated DOM evidence such as the provider composer or account-only marker. A timeout, network error, challenge page, cookie presence alone, or missing login control cannot prove either authentication state and remains `확인 중`.
- A positively authenticated login surface clears stale login-required UI, reports success once, refreshes settings state, and dismisses exactly once.
- Settings probes run sequentially so three provider WebViews are not kept alive at once.
- The reusable AIBI skill now requires the same closed-loop settings → login surface → positive evidence → exactly-once dismissal → refreshed settings contract.

### Verification

- iOS generic Simulator Debug build with one build worker — passed.
- Android focused ExternalAI unit tests — passed.
- Android `assembleDebug` — passed.
- AIBI runtime JavaScript, provider JSON, and frontmatter fallback validation — passed. The official skill validator could not start because the local Python environment lacks `yaml`/PyYAML.
- Matchup ledger structural validation — passed: 29 rows, 0 complete, 29 open.
- Matchup completion gate — intentionally open pending paired authenticated-device evidence.
- Android replacement installation — passed on SM-F968N after wireless ADB reappeared on a new port.
- Android attached provider probes resolved Gemini, ChatGPT, and Claude to `로그인됨` within eight seconds.
- Authenticated ChatGPT row tap returned to Settings automatically within three seconds and retained `로그인됨`.
- Root cause of the long `확인 중` state: Android created its status-probe WebView without attaching it to the Activity view hierarchy, so provider SPAs could skip layout/hydration. The probe now mounts off-screen with the iPhone reference viewport (375×667), then removes and destroys itself after resolution.
- The AIBI skill now rejects unattached settings probes and rejects any success callback that is immediately overwritten by a broad `확인 중` refresh.
- Android screen timeout and stay-awake state were verified as `2147483647` and `mStayOn=true`.
- Paired iPhone authenticated-device evidence is still pending, so the cross-platform Matchup completion gate remains open.

## iPhone login correction — 10:49–11:07 KST

- iPhone settings probes now mount one attached off-screen 375×667 WKWebView at a time instead of a 1×1 probe.
- Positive composer/account evidence resolves `로그인됨`; positive login URL/control resolves `로그인 필요`; challenge, network error, timeout, and unconfirmed states remain `확인 중`.
- An authenticated login sheet dismisses once and immediately keeps that provider row at `로그인됨` without a broad refresh overwriting it.
- Cancelled or replaced probe tasks now discard late JavaScript results and cannot advance or overwrite the active provider state.
- All login/status WKWebViews continue to share `WKWebsiteDataStore.default()` without reading or logging credentials, cookies, or tokens.
- Generic iOS Simulator Debug build passed with one build worker. Authenticated iPhone runtime timing and auto-dismiss evidence remain pending.

### Participation and usage evidence

- Android implementation: Gemini (`G`), followed by Codex (`TM`) source correction and verification.
- iOS implementation: Claude Sonnet (`C`), followed by Codex (`TM`) source correction and verification.
- Gemini five-hour remaining: 63.7118% → 60.2260%.
- Claude five-hour remaining: 95% → 95%; the end snapshot was stale by 768 seconds, so no finer conclusion is claimed.
- Codex weekly remaining: 97% → 96%.
