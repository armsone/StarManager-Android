# StarManager iOS → Android 동기화 보고서

- 시작: 2026-08-29 16:49:06 KST
- 종료 기준: 2026-08-29 17:42:58 KST
- 경과: 53분 52초
- iOS 기준: `/Users/armsone/git/StarManager` HEAD `704de24` 및 기존 미커밋 Automation Studio 변경
- Android 대상: `/Users/armsone/git/StarManager-Android` HEAD `540eb64` 위 작업 트리
- 참여: Gemini(기준 분석·초안), Claude Sonnet(초안 완결·계약/원장), Codex TM(검증·수정·빌드·설치)

## 동기화 결과

| 영역 | iOS 기준 | Android 반영 | 검증 |
|---|---|---|---|
| 자동화 기본값·설정 | 기본 꺼짐, 설정 토글 영속화 | `CreatorProfileStore`와 설정 화면에 반영 | JVM 테스트 통과 |
| 실행 트리거 | 콜드 실행·15초 이상 복귀 시 선택기 | `MainActivity` 생명주기와 전용 선택기에 반영 | 소스·빌드 통과, 수동 시간 경과 확인 대기 |
| 외부 사진 공유 | 공유 확장에서 1~8장 자동화 | `ACTION_SEND`·`ACTION_SEND_MULTIPLE` 처리 | 소스·빌드 통과, 실제 공유 확인 대기 |
| 카메라 바로가기 | 앱 바로가기에서 기존 카메라 진입 | Android launcher shortcut 연결 | 소스·빌드 통과, 롱프레스 확인 대기 |
| 공급자 선택 | Gemini·ChatGPT·Claude, Grok 제외 | `AutomationProviderSelector` 반영 | JVM 테스트 통과 |
| Automation Studio | 처리·결과·실패 전체 화면 | Compose 전체 화면과 숨김 AI WebView 연결 | 빌드·실행 통과, 실제 생성 흐름 확인 대기 |
| Instagram 전달 | 문구 복사와 1~8장 순서 공유 | FileProvider와 SEND/SEND_MULTIPLE 반영 | 빌드 통과, Instagram 실제 전달 확인 대기 |
| 인터스텔라 테마 | 다크 팔레트·아이콘 | 테마·컨트롤·런처 별칭에 반영 | 빌드·기본 화면 실행 통과, 래스터 비교 대기 |

## 검증

- `git diff --check`: 통과
- `./gradlew testDebugUnitTest`: 통과
- `./gradlew assembleDebug`: 통과
- `./gradlew lintDebug`: 통과
- 패리티 원장 구조: 18행 정상, 7행 완료, 11행 열림
- 패리티 게이트: 실패(실제 화면·공유·자동화 런타임 증거가 없는 항목을 `matched`로 올리지 않음)
- 실기기 설치·실행: SM-F968N, SM-F956N, SM-T500 모두 데이터 유지 교체 설치, versionCode `346633`, 실행 프로세스와 즉시 충돌 없음 확인

## 오류와 해결

1. Gemini 편집 호출이 고정 5분 제한을 넘어 종료됐다. 저장된 초안은 보존했지만 MainActivity·ComposerScreen 연결이 불완전했다.
2. Claude Sonnet이 누락된 진입 경로, 상태 연결, 계약과 원장을 보완했다.
3. 재발 방지로 `gemini-collab` 래퍼의 기본 제한을 15분 및 호출별 설정 가능으로 변경하고, 큰 작업을 독립 검토 가능한 기능 단위로 나누며 각 단계가 파일·잔여 작업·위험을 저장하도록 체크포인트 규칙을 추가했다.
4. Android 빌드·테스트·lint는 Codex TM이 직접 수행해 컴파일과 정적 오류를 확인했다.

## 남은 항목

- iPhone·Android 동일 픽스처로 Automation Studio 처리·결과·실패 화면 캡처 비교
- 15초 복귀, 공유 1장/8장, 카메라 바로가기, AI 3사 실제 응답, 119초 시간 제한, Instagram 전달의 실기기 상호작용 확인
- 기존 패리티 원장의 사진 방향·메모리·bitmap lifecycle 재확인
- 위 근거가 생길 때만 원장 상태를 `matched`로 변경

## 사용량 체크포인트

- 시작 시: Gemini 현재 세션 남은 약 79.6%(오래된 스냅샷), Claude 현재 세션 남은 81%, Claude Fable 남은 0%
- 종료 시: Gemini 현재 세션 남은 69.46%, 주간 남은 29.41%; Claude 현재 세션 남은 75%, 주간 남은 38%; Codex 주간 남은 63%
- Gemini 분석 호출 345,057 tokens, 편집 호출은 제한 종료 시 568,544 tokens로 기록됨
- Claude Sonnet 호출은 완료됐고 최종 래퍼 집계에서 output 60,042 tokens로 기록됨

## Git·공개 상태

- 최초 동기화 종료 시점에는 커밋·푸시·PR·릴리스·NasFinder.com 반영 없음
- iOS 기존 미커밋 변경과 다른 저장소의 사용자 변경은 보존함

## 후속 릴리스 후보 검증

- 고정 제품 버전: `2.5.1`
- 공통 표시 빌드: `202608291746`
- Android versionCode: `346666`
- `git diff --check`: 통과
- `./gradlew testDebugUnitTest`: 90개 테스트, 실패·오류 0
- `./gradlew lintDebug`: 통과
- `./gradlew assembleRelease`: 통과
- APK: `app/build/outputs/apk/release/app-release.apk`, SHA-256 `c5231e91bb4ca8b3e1d230137a7f70649965fcb7f2b2e45cefbec29024212f91`
- 데이터 유지 교체 설치·일반 실행: SM-F968N, SM-F956N, SM-T500 모두 `2.5.1 (346666)` 확인
- 명시적 카메라 콜드 인텐트: SM-F968N에서 기존 연속 촬영 화면과 `취소 / 0/8 / 완료 / 카메라 전환 / 사진 촬영` 의미 요소 확인
- 패리티 원장 구조: 18행 정상, 9행 완료, 9행 열림
- 이번 근거로 닫은 행: 자동화 기본 꺼짐·영속화, 공급자 목록의 Grok 제외
- 계속 열어 둔 행: 동일 픽스처의 영상 제외·실패 복구·사진 방향/메모리/bitmap 수명, 15초 복귀, 공유 1장/8장과 런처 롱프레스, 전체 화면 래스터 비교, Instagram 실제 전달
- 카메라 행은 명시적 인텐트 부분 근거를 추가했지만 복합 fixture의 공유·롱프레스가 남아 `needs_verification`을 유지함
